<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	   xmlns:mvc="http://www.springframework.org/schema/mvc"
	   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	   xmlns:context="http://www.springframework.org/schema/context"
	   xsi:schemaLocation="
			http://www.springframework.org/schema/beans
			https://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/mvc
			https://www.springframework.org/schema/mvc/spring-mvc.xsd
			http://www.springframework.org/schema/context
			https://www.springframework.org/schema/context/spring-context.xsd">
			
		<!-- 
			(참고) 컨텍스트 계층 구조 
	  	스프링 MVC에서는 두 계층의 컨텍스트가 존재
	  	
	  	1. Root ApplicationContext(부모)
			생성 주체: ContextLoaderListener
			역할: 공통 / 인프라 / 보안 / 서비스 전용 => 웹과 무관한 인프라 영역
			예: Service, DAO, Repository, DataSource 등 백엔드 공용 Bean     
			위치: /WEB-INF/spring/root-context.xml
			
			2. Servlet(Web) ApplicationContext(자식)
			생성 주체: DispatcherServlet
			역할: Controller / View / MVC 전용 => 웹 요청 처리 전용
			예: Controller, ViewResolver 등 웹 관련 Bean
			위치: /WEB-INF/spring/servlet-context.xml
			
			Root와 Servlet 둘 다 스프링 IoC 컨테이너이고, 부모-자식 관계
			여기서 IoC란? 객체 생성과 의존성 관리를 개발자가 아니라 컨테이너가 대신 수행하는 구조
	-->
	
	<!-- MVC 관련 어노테이션을 해석 및 실행하도록 필요한 인프라 Bean을 자동 등록 -->
	<mvc:annotation-driven/>
	
	<!-- Controller만 servlet-context, 나머지는 전부 root-context -->
	<context:component-scan base-package="org.zerock.controller" />
	
	<!-- JSP를 위한 ViewResolver 설정 -->
	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
	  <property name="prefix" value="/WEB-INF/views/"/>
	  <property name="suffix" value=".jsp"/>
	</bean>
	
</beans>